org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# jvplayer-kit publishing coordinates (plan §2.4). SemVer; all artifacts share one version.
# 1.0.0 = W4 API freeze (APP-589): :player-api is the frozen contract — see docs/api-stability.md.
jvplayer.group=com.jv.player
jvplayer.version=1.0.0
